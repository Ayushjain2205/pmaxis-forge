# PMAxis — required for dsh --profile pmex (MCP stdio server).
PMAXIS_API_KEY=

# OpenRouter — paste on the desk Keys sheet, or set this (env wins).
# OPENROUTER_API_KEY=

# Official DeepSeek (optional if you use OpenRouter instead).
# DEEPSEEK_API_KEY=
